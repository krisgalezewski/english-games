# Gameplay Audit (Job 2)

One row per game: what was checked, what was found, what was fixed. Checklist applied to every
game (per `design_handoff/README.md` §Job 2, expanded with Kris's own priorities):

- Start/restart hygiene (timers, listeners, score reset)
- Timers cleaned up on end/restart/`visibilitychange` (backgrounded tab doesn't burn the clock)
- Answer checking: trim/case-normalise, legitimate alternative answers accepted
- Question/word pool: no repeats within a run, real shuffling (Fisher–Yates)
- Edge inputs: empty submit, Enter-to-submit, double-click, answering after time expires
- Scoring: streak resets on wrong answer, submitted score == displayed score
- Mobile: touch targets ≥44px, touch controls on canvas games
- Accessibility: keyboard reachability, focus rings, `aria-live`, reduced-motion
- Pausing works and the game screen is prioritised while a run is in progress (tab-blur behaviour)
- Mode switching (where applicable) is clean — no state bleed between modes
- Leaderboard: score submits correctly, name persists, table renders correctly
- Start/greeting screen gives clear, complete, user-friendly instructions
- Content accuracy: lexical/grammatical correctness, no ambiguous questions, exhaustive accepted-answer sets

Status legend: 🔍 in progress · ✅ audited, clean · 🛠 audited, fixed · ⚠️ audited, issue flagged (not yet fixed)

| Game | Status | Found | Fixed |
|---|---|---|---|
| shared.js (leaderboard system, all games) | 🛠 | The Supabase-backed leaderboard (`submitScore`/`getLeaderboard` in `shared.js`) is intact and live — confirmed by loading the *production* site in a real browser and seeing real student rows (names/scores) render on PhrasalBlitz's board, so nothing about it broke in the Job 1 redesign. Two real bugs found: (1) `handleScoreSubmit` (used by ~12 of the 14 games) had no guard against rapid double-click/double-tap — two quick clicks fired two separate POSTs to Supabase, i.e. could add the same score twice. (2) On a failed submission it replaced the entire form with just an error message, so the player's name was lost and there was no way to retry without restarting the round. (3) Its confirmation/error text used the old theme's bare `.eyebrow` class, which has no styling at all in the new `styles.css` (unlike `--correct`/`--wrong`/`--muted`, which were already aliased for backward compatibility) — so "✓ Score submitted!" rendered as unstyled plain text instead of the small-caps label used everywhere else. Separately, **Affix Runner** and **English Ace** don't use `handleScoreSubmit` at all — they call `submitScore` directly with no confirmation message, no error feedback, and no double-submit guard, so a player has no way to know whether their score actually saved. Also noted: `renderCompactEndCard` in `shared.js` is dead code (zero callers, and built entirely from pre-redesign classes with no styles left) — harmless but worth deleting or repurposing. | Added an in-flight guard to `handleScoreSubmit` (disables the form's controls immediately, ignores a second call while one is pending). On failure it now re-enables the form and appends an error message instead of destroying the player's input. Fixed the `.eyebrow` → `.eg-eyebrow` class. Gave Affix Runner and English Ace the same treatment directly (confirmation text, error text, disable-while-submitting, double-submit guard) since they don't route through the shared helper. Verified via Playwright with a mocked `submitScore` (can't reach the real Supabase endpoint from this sandbox's network) that a rapid double-click results in exactly one call in all three code paths, and that a failed submission leaves the form usable for a retry. Live leaderboard read confirmed by loading the real production PhrasalBlitz page in a real browser. |
| index.html (entry flow) | 🛠 | All 14 game links resolve correctly; nav anchors (#fast-lane/#arcade/#workshop/#long-game/#leaderboard/#gotw) all hit real elements; game-of-the-week rotation logic (ISO week, ` % 14`) is sound and SVG art swap works; no console/page errors. The leaderboard tile's "Open the board →" was `href="#"` — a dead link, since there was no cross-game leaderboard page. | Kris asked for a real cross-game leaderboard — built `leaderboard.html` (see its own row below) and pointed the tile at it. |
| leaderboard.html (new — cross-game hub) | 🛠 | New page. Fetches the whole Supabase `leaderboard` table once (`select=game,player_name,score&limit=5000`) and does two things client-side: (1) a "Most active players" ranking — total scores submitted per player across every game, which is the one metric that's actually fair to compare cross-game (raw scores from a 60-second PhrasalBlitz round and a WordSnake run aren't comparable, so the page deliberately does **not** rank players by summed/combined score); (2) a card per game showing that game's own current top score, linking through to the game's own full board to play/submit. Game→Supabase-key matching is prefix-based (`row.game === key \|\| row.game.startsWith(key + '_')`) so multi-mode games (PhrasalBlitz's 3 modes, Affix Runner's easy/hard, etc.) are correctly folded into one "best score" per game rather than shown as separate unrelated rows. | Built and verified with Playwright by intercepting the Supabase request with mocked multi-game, multi-mode data: confirmed the per-game "best score" picks the true max across modes (e.g. Affix Runner easy=300/hard=410 → shows 410), games with zero rows show "No scores yet — be first!" instead of breaking, the most-active ranking counts correctly, and a failed fetch degrades to a usable page (grid still renders and links work) rather than a blank screen. Reused the same `.eg-card`/`.eg-lb-table`/`.eg-tile--teal-deep` patterns as the rest of the site — no new CSS beyond page-local layout rules. **Caveat:** fetches up to 5000 rows in one request and ranks/groups all of it client-side; fine for a single class's leaderboard, would need real pagination if the table grows far beyond that. |
| PhrasalBlitz | 🛠 | Structural: (1) `ALL_PARTICLES` was a hand-maintained 20-item list used only to generate wrong-answer distractors in Mode 1 — it silently drifted out of sync with the 115-verb/701-pair dataset (missing common particles like "to", "round", "against", "together", "aside", multi-word ones). It never blocked a *correct* answer (that check is always against the verb's own curated list), but it did shrink the distractor pool. (2) No `visibilitychange` handling — the 60s timer kept counting down in a backgrounded tab. (3) Zero `aria-live` regions anywhere — wrong/correct feedback (colour flash + shake) had no text equivalent for screen readers. Content: spot-checked ~15 of the 115 verbs against real-world usage and found 2 genuine gaps — "cut through" and "keep off" were real, commonly-used phrasal verbs missing from their verb's accepted-answer list, so typing them was (correctly, per the game's own data, but wrongly per real English) marked incorrect. **Not fully verified:** a true line-by-line check of all 701 verb+particle pairs against a phrasal-verb dictionary is a much bigger undertaking than one session — flagging as ongoing. | Derived `ALL_PARTICLES` dynamically from the dataset instead of hand-maintaining it. Added the missing "cut through" and "keep off" entries with definitions/examples matching the existing style. Timer now skips its tick while `document.hidden`, so switching tabs no longer burns the clock. Added a shared `.sr-only` utility in `styles.css` plus a `#srStatus` `aria-live="polite"` region wired into every correct/incorrect/round-complete event. Verified via Playwright: new entries are reachable and score correctly in both Mode 1 (click) and Mode 3 (type), timer freezes while hidden and resumes on return, restart resets score/found to 0, no console errors. |
| CollocationCrash | 🛠 | Mechanics are solid: no timer (so no visibilitychange concern), `answer()` disables all options synchronously on the first click so a rapid double-click can't double-score, restart (`initRound`) fully resets score/round-index/results with a genuinely fresh random question set (Fisher–Yates, 10-day repeat avoidance via `freshRandomIndices`), and mode switching (Easy/Hard, both the pill tabs mid-game and the end-card button) cleanly swaps the question bank and starts a new round with no state bleed. No `aria-live` on the answer feedback. Content: re-checked the ~150 verb+noun questions against real usage (building on last session's ambiguity fix) and found 2 more cases where a "wrong" distractor was actually also valid, standard English — "a course": marking **"do"** wrong when "do a course" is standard BrE (only "take" was accepted); "the dishes": marking **"wash"** wrong when "wash the dishes" is just as standard as "do the dishes". Both would have penalised a student for a genuinely correct answer. **Flagging, not changing:** "a policy" offers "enforce" as a wrong distractor — "enforce a policy" is also completely natural English, just a different meaning (ensuring compliance vs. introducing it) rather than a synonym, so it's a softer call than the two fixed above; wanted your read on it rather than unilaterally deciding. **Not fully re-verified:** same caveat as PhrasalBlitz — didn't re-derive all ~150 questions from scratch against a dictionary, just re-scanned with an eye for this specific failure mode. | Swapped "do"→"make" as the "a course" distractor and replaced "the dishes"'s distractor set (`wash`/`clean`→`have`/`take`, matching the safe pattern already used by its sibling "the shopping"/"the cooking" questions). Added a short note to each question's explanation acknowledging the near-miss alternative, so a curious student isn't left thinking the alternative is flatly wrong. Added `aria-live="polite"` directly to the feedback block. Verified via Playwright: the two fixed questions' distractor sets no longer contain the ambiguous verb, a full 10-question round can be played to a perfect 100/100 score, restart resets to a fresh round, switching Easy→Hard swaps to the 106-question hard bank cleanly, no console errors. |
| SpellIt! | 🛠 | Mechanics are solid: untimed (so no visibilitychange concern), restart/mode-switch (`setDiff`) fully re-run `initRound()` with a fresh Fisher–Yates/10-day-repeat-avoidance word set, `handleScoreSubmit` already gives it the shared double-submit guard from the shared.js pass. One real logic bug: the "Extra hint" resource is meant to be usable once **per word**, but its visibility flag (`extraHintUsed`) was only reset in `initRound()` (once per round) instead of in `loadWord()` (once per word) — so using it on any single word permanently hid the Extra-hint button for every remaining word in that round. No `aria-live` on the feedback text. Content: this game's whole premise is "type the correctly spelled word," which makes it the single highest-risk game in the audit for Kris's exact complaint (a correct answer marked wrong) — several of its own hint texts *literally flag* a British/American spelling difference (e.g. PROGRAMME's hint says "British spelling", ARCHAEOLOGICAL's and ENCYCLOPEDIA's hints call out the alternate spelling tradition) while `checkSpelling()` only ever accepted one exact, hardcoded spelling. Found 5 words where the real, standard alternate-dialect spelling would have been marked wrong: PROGRAMME (BrE target) rejecting PROGRAM (AmE); NEIGHBOUR (BrE) rejecting NEIGHBOR (AmE); JUDGMENT rejecting the more common general-British JUDGEMENT; ENCYCLOPEDIA rejecting the traditional/British ENCYCLOPAEDIA; ARCHAEOLOGICAL rejecting the standard American ARCHEOLOGICAL. **Not fully re-verified:** didn't cross-check all ~168 words in both banks for every possible BrE/AmE pair — flagging as ongoing, same caveat as PhrasalBlitz/CollocationCrash. | Added an `alts:[...]` field to each of the 5 affected word entries and updated `checkSpelling()` to accept any listed alternate spelling (case-insensitive) in addition to the primary one — a genuinely wrong answer is still rejected as before. Fixed the Extra-hint reset bug by moving `extraHintUsed = false` into `loadWord()`. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: all 5 alternate spellings now score as correct, a real misspelling ("NESSECARY") is still rejected, the Extra-hint button correctly reappears on the word right after being used (previously stayed hidden), a full 8-word round can be played to completion and submitted, restart resets score to 0, and switching Easy→Hard mid-game cleanly swaps to `HARD_WORDS`. No console errors beyond the sandbox's known inability to reach Supabase. |
| Dictation Drill | 🛠 | This one already carried several careful prior fixes (code comments document them): the countdown only starts once the first playback genuinely finishes, and a replay freezes/resumes the countdown rather than burning time. Two structural gaps remained despite that care: (1) no `visibilitychange` handling at all — same class of bug as PhrasalBlitz's original timer — so the countdown kept ticking in a backgrounded tab. (2) `advanceItem()` bumps `current` *immediately* (before the item actually changes on screen a moment later via a `setTimeout`), and nothing blocked Submit/Skip/Hint/Replay from firing again during that gap — a stray Enter-key repeat or a fast double-click on Skip during that window would silently act on the *next, not-yet-shown* item (e.g. double-charging a skip, or flashing a spurious "Not quite" over what was actually a correct answer). No `aria-live` on the feedback text. Content: re-checked all four word/phrase banks (120 minimal-pair words, 76 tricky-spelling words, 120 connected-speech phrases, 20 full sentences) specifically for Kris's exact complaint pattern — a legitimate answer marked wrong — and found it in the same shape as SpellIt!: the tricky-spelling bank includes "neighbour" and "favourite" (British-only spellings), and two connected-speech phrases contain "colour", all compared with a strict exact-match checker that only accepted the one spelling on file, so a student typing the standard American spelling of a word they heard correctly would be marked wrong. | Added a shared `timerTick()` used by both the normal countdown and the post-replay resume, which skips the tick entirely while `document.hidden` — tab-switching no longer burns the clock. Added an `advancing` flag set the instant an item is resolved (correct/skip/timeout) and cleared only once the next item actually loads; Submit, Skip, Hint and Replay all now bail out immediately if it's set, closing the double-action race in all four places at once (also disabled the text input itself during that same window as a visible/defense-in-depth cue). Extended `normaliseForCompare()` to canonicalise "colour"→"color", "neighbour"→"neighbor" and "favourite"→"favorite" on both sides of every comparison (word answers and full-phrase answers alike), so either spelling now scores correct while a genuinely different word is still rejected exactly as before. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: American spellings now score correct in both a plain word item and a connected-speech phrase, a truly wrong word is still rejected, the timer provably holds while `document.hidden` is true and resumes after, rapid-fire submit/skip calls during the advance window are fully absorbed (skip count and results array each only move once), a full 10-item easy round can be played to 10/10 and submitted, restart zeroes the score, and Easy→Medium mode switching cleanly swaps word banks. No console errors beyond the sandbox's known inability to reach Supabase. **Not fully re-verified:** didn't cross-check every one of the ~336 total bank entries against a dictionary for other possible dialectal variants beyond the three found — flagging as ongoing, same caveat as the other content-heavy games. Also worth noting for the record (not a bug, inherent to the format): this is the one game in the set that depends entirely on the browser's speech-synthesis voice being intelligible, so its accessibility ceiling is different from the text-based games — nothing to fix here, just a limitation of a listening exercise by definition. |
| WordChain | 🛠 | Solid foundations: word validity is checked against a real, comprehensive dictionary fetched at load time (with a sizeable embedded fallback list if the fetch fails), so — unlike the hand-curated answer lists in PhrasalBlitz/CollocationCrash — there's no small-list ambiguity risk here; restart/mode-switch (`startChain`) fully reset lives/score/chain/timer with no bleed between runs; every rejection path already re-renders and visually flashes the letter/POS prompt so the player is never confused about what's still required after a wrong guess. Found: (1) no `visibilitychange` handling — same class of bug as every other timed game audited so far — so the 60s clock kept running in a backgrounded tab. (2) Hard mode's part-of-speech check calls a live third-party dictionary API and awaits the response with **no submission lock** — a fast player who typed and submitted a second word while the first word's network check was still in flight could get both words scored for the same "turn," since neither `chain` nor `currentLetter` had been updated by the time the second call ran its own (synchronous) validation. A related edge case: if the round's timer expired while a Hard-mode check was still in flight, the check could resolve *after* the end card was already showing and silently mutate the (now stale) game state. (3) The one rejection branch that changes the requirement being asked for — a wrong part of speech, which picks a fresh POS for the retry — was missing the `flashPrompt()` visual highlight that every other rejection branch already gets, even though the requirement changing is exactly when a player is most likely to miss the update. No `aria-live` on the feedback text. | Added a `document.hidden` guard to the timer tick. Added a `submitting` lock that's held for the full duration of a word submission (including the async POS check) so a second submission during that window is a no-op, plus a `gameOver` check right after the POS check resolves so a check that outlives its own round can no longer mutate a state that's moved on. Added the missing `flashPrompt()` call to the wrong-POS rejection branch. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: a real 3-word chain scores correctly with no lives lost, a wrong-letter word and a repeated word each cost exactly one life without changing the required letter, the timer provably holds while `document.hidden` and resumes after, two overlapping Hard-mode submissions (one still "checking" when the second fires) now only ever score the first one, and restart fully resets lives/score/chain. No console errors beyond the sandbox's inability to reach GitHub's raw content host and Supabase (both fine on the real deployment; the game's own fallback word list covers exactly this case). |
| Word Builder | 🛠 | This game turned up the most significant content bugs of the audit so far, all through a fully programmatic check (loaded a real system dictionary and cross-checked every word in all 56 curated racks against both letter-availability and real-word status — not just spot-checking). Found: (1) Two entire racks (`CDEHIRT`/hard and `ABEGNRS`/hard) were each missing a single letter (A and I respectively) that roughly half of their own curated word list needed — e.g. "ACHE", "CARE", "TRADE" on a rack with no A; "AIR", "GRIN", "BEGINS" on a rack with no I. Every one of those words would show up as "missed" at the end of every single round of those two racks, forever — genuinely impossible for any player to ever find, exactly the kind of unfair/impossible content Kris flagged as a concern. (2) ~20 more one-off words scattered across 9 other racks that needed a letter simply not present in that rack (e.g. "ACORN" on a rack with no N, "MAP" on a rack with no P) — smaller versions of the same bug. (3) 10 words that don't exist in English at all (STELAR, MOAR, ROMANE, THRICED, ONICE, ELOTION, GILDAR, LIGARD, LINGHES, INALS, SAB) — cross-checked against two full system dictionaries (American + British, ~180k words combined) with zero matches; likely typos/scrambles that crept into the curated lists. (4) The word-validity check has a **fallback dictionary** for when the live GitHub word-list fetch fails (same network dependency risk as WordChain) but the original fallback logic was dictionary-OR-nothing: when a dictionary *did* load (fetched or fallback), the checker used **only** that dictionary and completely ignored the rack's own curated list — so a curated word missing from whichever dictionary happened to load would be wrongly rejected even though it's sitting right there in the rack's own "correct answers" list. (5) No `visibilitychange` handling on the 90s round timer (same class of bug as every other timed game), and the same duplicated, unguarded timer-restart logic in the "resume after reading a definition" flow. No `aria-live` on the feedback text. | Restored the missing letter on both broken racks (verified every one of their words is now buildable with nothing else needing to change) and trimmed the ~20 one-off impossible words and the 10 non-words from the other racks — reverified programmatically afterward that zero words in any of the 56 racks are impossible to build from their own rack, across both Easy (letter-reuse) and Hard (one-use) rules. Gave WordBuilder the same broad embedded fallback word list WordChain already uses, so a blocked GitHub fetch degrades to a real ~2,500-word dictionary instead of an empty one. Changed the validity check to accept a word if it's in **either** the rack's own curated list **or** whichever dictionary loaded (previously dictionary-only whenever one loaded) — this on its own caught and fixed a second bug my first fix introduced: several very common curated words (ALE, LATE, RATE, SEAT, TALE) aren't in the smaller embedded fallback list, so with the old "dictionary-only" logic they'd have started failing only in the fallback-network scenario. Added a shared `timerTick()` (skips the tick while `document.hidden`) used by both the main round timer and the post-definition-modal resume timer. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: the two previously-impossible words (ACHE, GRIN) now build and score correctly on their fixed racks, a full programmatic sweep confirms zero impossible words remain across all 56 racks, a genuinely nonsense combination is still rejected, the timer provably holds while `document.hidden` and resumes after, and a full start-game-submit-a-real-word playthrough works end to end. **Not fully re-verified:** the ~460 curated words not present in the smaller embedded fallback dictionary (mostly less-common real words) were spot-checked rather than each hand-verified against a dictionary — the union-based fix means this no longer matters for correctness (the curated list covers them either way), but flagging for completeness. |
| Anagram | 🛠 | Mechanics were mostly solid: restart/mode-switch (`setDiff`) fully re-run `initRound()` with a fresh Fisher–Yates/10-day-repeat-avoidance word set, and a programmatic check of every word in both `EASY_WORDS` and `HARD_WORDS` found zero duplicate words and zero anagram-signature collisions within either bank (i.e. no two words in the same bank scramble to the same letter set, which would make the puzzle genuinely ambiguous). Two real bugs found: (1) no `visibilitychange` handling on the 90s round timer — same class of bug as every other timed game audited so far — so the countdown kept running in a backgrounded tab. (2) A correct answer is scored immediately in `checkAnswer()` but the actual advance to the next word (`current++; loadWord()`) is deferred behind a 900ms `setTimeout` (presumably so the player sees the green "correct" flash) — and nothing blocked `checkAnswer()`, `showHint()`, or `skipWord()` from firing again during that 900ms window. An Enter-key repeat or a fast double-click on Check right after solving a word would silently re-run the correct-answer branch against the *same, already-solved* word (since `current` hadn't advanced yet), awarding a second helping of points and pushing a second, duplicate entry into the results/review list — exactly the kind of scoring bug Kris asked to be checked for site-wide. No `aria-live` on the feedback text. Considered but deliberately not treated as a bug: some words in the same bank share letters with each other (e.g. a bank containing both MARCH and CHARM) — this is fine and not ambiguous, because each puzzle shows its own definition, which uniquely identifies the one correct target word regardless of what other words could theoretically be built from the same letters. | Added `if (document.hidden) return;` as the first line of the timer's tick, so tab-switching no longer burns the clock. Added an `advancing` flag set the instant a correct answer is scored (before the 900ms delay) and cleared only once the next word actually loads; `checkAnswer()`, `showHint()`, and an unforced `skipWord()` now all bail out immediately if it's set, closing the double-scoring race. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: calling `checkAnswer()` three times in rapid succession right after a correct answer only ever scores and records the word once (not three times), the round still advances by exactly one word, calling `skipWord()`/`showHint()` during that same 900ms window is fully blocked (no extra result pushed), the timer provably holds while `document.hidden` is true and resumes after, a full 8-word round can still be played to completion via forced skips and shows the end card, restart zeroes score/current/`advancing`, and Easy→Hard mode switching cleanly resets state and keeps the timer running. No console errors beyond the sandbox's known inability to reach Supabase. |
| WordSpark | 🛠 | Note: the live "WordSpark" game is `synonyms.html` (Supabase keys `wordspark_syn`/`_ant`/`_type`) — the separate, orphaned `wordspark.html` was already deleted earlier in this audit (confirmed with Kris). Full gameplay audit now complete. This is a well-built game (a 53-word syn/ant bank across three modes — pick-the-synonym, pick-the-antonym, and type-it — with a 60s countdown and a −5s penalty for a wrong guess) and, unlike several other games audited, its 53-entry word bank turned up **zero** structural content bugs on a full programmatic sweep (no duplicate words, no word duplicated within its own syn/ant list, no word appearing as both a synonym and antonym of the same target, no self-reference) — the reciprocal design (e.g. "bold" and "timid" correctly listing each other back) shows real care. Two small, genuine content gaps: the words "bold" and "subtle" are used as antonym values (of "timid" and "obvious" respectively) but had no entry in `WORD_EXAMPLES`, so clicking either as a found chip would show the *parent* word's example sentence instead of one for the word actually clicked. Also found two dead, unreachable keys (`simple_adj`, `unassuming_adj`) in `WORD_EXAMPLES` — a leftover attempt to give "simple" and "unassuming" a second, context-specific example that never actually connects to anything, since `showWordModal` only ever looks up the bare word string. Mechanics: (1) no `visibilitychange`/hidden handling on the 60s timer — same class of bug as every other timed game in this audit, so a backgrounded tab let the clock run down for free. (2) The more interesting bug: an **advance-window race present in all three modes**, the widest-reaching version of this bug pattern found so far in the audit. In Modes 1/2 (button grid), a correct click schedules the next word via `setTimeout(…, 700)` but never disabled the grid in the meantime — a fast double-click on the just-answered correct button re-ran the scoring branch a second time, which (harmlessly) no-opped on score/found thanks to the existing `allFound` guard, but still re-scheduled a *second* `pickNextWord()` call, silently skipping an extra word out of the queue on every such double-click. In Mode 3 (type it), the bug was more serious: `submitTyped()`'s own top line *cancelled* any pending flash-timer and reprocessed the input — so a fast repeat Enter/Check press on a wrong guess re-penalised the same still-visible wrong attempt a second time (double time-loss for one mistake), and worse, a repeat press right after a *correct* answer re-validated against the just-updated `found` set (which now excludes the word just scored), which could flash a false "wrong" and cancel the scheduled advance entirely — potentially stranding the round on an already-solved word. Hint/Extra-hint/Skip in Mode 3 had the same gap: clicking any of them during the pending correct/wrong flash spent real points/skip-currency on a word that was about to change anyway. No `aria-live` anywhere — every signal (button flash, letter-box colour, penalty screen-flash) was purely visual. | Added `if (document.hidden) return;` to the timer tick. Added a shared `advancing` guard for Modes 1/2, set the instant a correct click registers and cleared at the top of `pickNextWord()`; `handleOpt1`/`handleOpt2` now bail out immediately if it's set, so a rapid re-click on an already-scored button can no longer double-schedule the next word. For Mode 3, replaced `submitTyped()`'s cancel-and-reprocess opening with a guard that blocks re-entry entirely while a previous check's flash (`typeFeedbackTimer`) is still pending — this fixes both the wrong-answer double-penalty and the correct-answer false-negative/cancelled-advance bug in one change — and added the same guard to `showHint()`, `showExtraHint()`, and `skipTypeWord()` so none of them can act during that same window. Added a visually-hidden `#srStatus` `aria-live="polite"` region with an `announce()` helper wired into every correct find (across all three modes), every wrong guess (via a single call inside the shared `penalise()`, covering all three modes at once), and the end-of-game summary. Added the two missing `WORD_EXAMPLES` entries for "bold" and "subtle", and removed the two dead unreachable keys. Verified via Playwright (9 assertions plus a standalone Mode 2 check): `aria-live="polite"` present; a full programmatic content sweep confirms zero structural issues and zero missing/dead `WORD_EXAMPLES` entries; triple-clicking a just-answered correct button in both Mode 1 and Mode 2 scores and advances exactly once (confirmed by instrumenting `finalScore`/`foundItems` deltas); triple-submitting one wrong Type-It answer calls `penalise()` exactly once (confirmed via a call-count spy, since the live countdown otherwise pollutes a naive before/after time comparison); triple-submitting one correct Type-It answer scores and advances exactly once; clicking Hint during a pending flash spends no points and doesn't mark the hint used; the timer provably holds while `document.hidden` and resumes after. No console errors beyond the sandbox's known inability to reach Supabase. |
| Conditional Constructor | 🛠 | This is the most content-heavy game audited so far (250 hand-written questions across 12 conditional-type categories, plus drag-and-drop mechanics) and turned up its most severe bug of the whole audit: the **`condq`** question type (20 "conditional phrased as a question" items, e.g. "What would you do if you won the lottery tomorrow?") had no matching entry in the `TYPES` lookup object used to render the type badge/colour/rule text. Since `condq` is one of the four types `EXPERT_PRIORITY` *guarantees* appears at least once in every single Expert-mode round, this meant **Expert mode was reliably crashing** (`Cannot read properties of undefined`) on whichever question in the round happened to be a `condq` item — a student playing the hardest, most advanced difficulty would have hit a broken game on a very high proportion of their attempts. Also found: (1) no `visibilitychange` handling on the round timer — same class of bug as every other timed game. (2) A real restart-hygiene bug: the end-card's "Play again"/"Switch level" buttons each had both a working `onclick` attribute *and* a separate delegated click listener that called the exact same function a second time on every click — harmless to the score (both functions are idempotent) but it silently doubled the number of drop-zone event listeners added on every restart (see next point) and did twice the DOM work for no reason. (3) The drag-and-drop zone's `dragover`/`dragleave`/`drop` listeners were re-attached to the same static `#dropZone` element inside `loadQuestion()` — called once per question — instead of being set up once, so listeners piled up indefinitely over a round (worse still, doubled again by bug #2 above); each stacked `drop` listener still correctly no-ops once `answered` is true, so it never caused a *scoring* bug, but it's a genuine, unbounded memory/listener leak over a long play session. (4) The draggable answer options are plain `<div>`s (needed so they can also be native HTML5 drag sources) with only mouse/touch `click` and `drag` handlers — no `tabindex`, no keyboard handler — so a keyboard-only student could not select an answer at all; this is a real accessibility regression relative to the rest of the site, since the identical `.eg-option` class is a real `<button>` (natively keyboard-operable) in every other game that uses it, e.g. CollocationCrash. No `aria-live` on the feedback panel. Content: ran a full programmatic sweep of the 250-question bank checking for duplicate/self-matching answer options (a wrong distractor accidentally identical to the correct answer, or two identical distractors in one question) and missing fields — found zero. Manually sampled roughly 90 questions across every conditional type, including the trickiest mixed/inversion categories, specifically hunting for Kris's exact complaint pattern (a distractor that's actually also grammatically valid, or an ambiguous prompt) — found none; the distractor design (wrong tense pairing, wrong time reference) is careful and consistent throughout. **Not fully re-verified:** the remaining ~160 questions weren't individually hand-checked against a grammar reference — flagging as ongoing, same caveat as the other content-heavy games. Also noted, not changed: Practice mode only offers the six "basic" conditional types (zero/first/second/third/m32/m23) — the five inversion forms and the `condq` "question" type can only ever be encountered inside a fast, scored, 120-second Expert round, never practised in isolation untimed. That may well be an intentional design choice (Expert *is* meant to be where those forms live), but it means a student who specifically wants to drill "Had I known..." inversions has no dedicated practice card for it — flagging for your call rather than unilaterally adding new practice categories. | Added a `condq` entry to `TYPES` (label "Conditional question", a new distinct colour `--cq`, and an explanatory rule string) — this was the critical fix, since it's what was actually crashing Expert mode. Added `if (document.hidden) return;` to the timer tick. Removed the redundant delegated click listener on `#endCard` (the inline `onclick` handlers already do the job on their own). Moved the drop-zone's `dragover`/`dragleave`/`drop` listeners out of `loadQuestion()` into a one-time `setupDropZone()` call at script boot, so they're attached exactly once for the life of the page instead of accumulating every question. Gave every option card `tabindex="0"`, `role="button"`, and a keydown handler (Enter/Space triggers the same selection as a click) — styles.css already had an unused `.eg-option:focus-visible` outline rule ready and waiting for this. Added `aria-live="polite" aria-atomic="true"` to both the main feedback panel and the practice-mode feedback panel. Verified via Playwright: a full 10-question Expert round — guaranteed to include a `condq` question — now completes end-to-end with zero crashes and a real `TYPES.condq.color` value (previously threw `TypeError` on the type-badge render); a keyboard `Tab`+`Enter` sequence now correctly selects and submits an answer with no mouse involved; clicking "Play again" once now resets state exactly once (score/current both zero, not corrupted by a phantom double-init); 5 sequential correct answers produce exactly 5 results (no duplicate scoring from stacked drop listeners); the timer provably holds while `document.hidden` is true and resumes after. No console errors beyond the sandbox's known inability to reach Supabase. |
| Irregular Verb Sprint | 🛠 | This game had clearly already had a prior accessibility/hygiene pass (its own code comments note "single listener, no accumulation" for the global Enter-key handler, and it already accepts BrE/AmE spelling-variant answers like burnt/burned, learnt/learned via a 32-entry alternatives table) — but two real bugs remained, one of them genuinely user-visible. (1) The end-card's "Play again"/"Switch mode" buttons each had both a working `onclick` attribute *and* a separate delegated click listener calling the same function a second time on every click — for "Play again" this was harmless (restarting twice is a no-op difference), but for **"Switch mode" it was a real, reproducible bug**: the delegated listener's own `currentMode==='easy'?'medium':...` ternary is evaluated fresh on every click, and by the time it ran, the *first* (inline) call had already changed `currentMode` — so a single click cycled the mode two steps instead of one (e.g. clicking once in Easy mode jumped straight to Hard, skipping Medium entirely). (2) No `visibilitychange` handling on the round timer — same class of bug as every other timed game. (3) `submitAnswer()`, `useHint()` and `skipVerb()` had no guard against being called again during the 600–1500ms `advanceVerb()` delay between a scored answer and the next verb actually loading (the classic advance-window race already found and fixed in several other games this audit) — a fast Enter-key repeat right after a correct/wrong answer could re-score the same already-answered verb and push a duplicate entry into the results/review list. No `aria-live` on the feedback text. Content: ran a full programmatic sweep of all 104 verbs — zero duplicate base verbs, all `ps`/`pp` fields present, every one of the ~300 Hard-mode fill-in-the-blank sentences has exactly one blank and a valid `ps`/`pp` field tag — then cross-checked every sentence's surrounding auxiliary verb (has/have/had/was/were/etc.) against its declared field to catch a mislabelled blank; found one real (if low-impact) case: "cut" — tagged as needing the past *simple* for "The film was **___** by almost twenty minutes," a passive-voice sentence that's actually demonstrating the past *participle*. Since "cut" is one of the invariant verbs (base=cut, past simple=cut, past participle=cut), this never affected what counted as a correct answer, just the on-screen "Past simple"/"Past participle" label shown next to the input. | Removed the redundant delegated `#endCard` click listener (the inline `onclick` handlers already do the job on their own) — this is the fix for the mode-skipping bug. Added `if (document.hidden) return;` to the timer tick. Added an `advancing` flag set inside `advanceVerb()` and cleared at the top of `loadVerb()`; `submitAnswer()`, `useHint()` and `skipVerb()` now all bail out immediately if it's set, closing the double-scoring race. Relabelled the one mistagged "cut" sentence from `ps` to `pp` to match its passive-voice construction. Added `aria-live="polite" aria-atomic="true"` to `#feedback`. Verified via Playwright: calling `submitAnswer()` three times in rapid succession right after a correct answer only ever scores and records it once, and the round still advances by exactly one verb; calling `useHint()`/`skipVerb()` during that same delay window is fully blocked; a single click on "Switch mode" now moves exactly one step (Easy→Medium, not Easy→Hard) — confirmed the pre-fix code really did double-cycle by reproducing it before applying the fix; a single click on "Play again" resets score/current to zero exactly once; the timer provably holds while `document.hidden` is true and resumes after. No console errors beyond the sandbox's known inability to reach Supabase. |
| WordSnake | 🛠 | The most different game in the set architecturally (a real-time canvas snake game, `requestAnimationFrame`-driven, not a question-by-question quiz), and it's noticeably better-engineered than most of the others out of the gate: the tick loop only ever advances by exactly one step per frame no matter how much real time has elapsed since the last one (`lastTick = ts`, not accumulated), so there's no "catch-up burst" risk on resume; manual pause (`togglePause()`) correctly re-anchors `lastTick` on resume so un-pausing never counts the paused time as elapsed; word-banking and letter-erasing are fully synchronous with no `setTimeout`-delayed state changes, so none of the "double-action during an advance window" races found in the other games apply here; and it already has full keyboard controls (arrow keys/WASD to steer, Enter to bank, R/N for resets, Space/Escape/P to pause) as well as touch swipe/tap controls, so the on-screen D-pad being mouse/touch-only isn't an accessibility gap — keyboard users have complete parity through the keys, the D-pad is just the mobile-friendly extra. Two real gaps found: (1) no `visibilitychange` handling at all — since this is an endless-survival game with no round timer, the risk isn't "the clock burns for free," it's that the snake keeps moving and can wrap around into itself while the tab is hidden and the player can't see or react, silently costing one of their 5 limited snake-resets. (2) No `aria-live` region anywhere — the toast messages ("+12 pts — CRANE", "Tangled up! Snake reset used", "⚡ 5 words! Snake speeding up...") are the game's only feedback channel and were entirely inaccessible to screen readers. Content/mechanics: the letter-tile distribution and per-letter point values are a deliberate, reasonable Scrabble-style design (not a content bug); the word-validity dictionary uses the same live-fetch-with-embedded-fallback pattern already audited in WordChain/Word Builder, and correctly checks the fallback set regardless of whether the live fetch succeeded (an unused `dictLoaded` flag is harmless dead code, not a bug). | Added a `visibilitychange` listener that calls the existing `togglePause()` the moment the tab is hidden (mid-game, not already paused) — reusing the same pause machinery a manual Space/Escape/P press uses, so `lastTick` is correctly re-anchored whenever the player manually resumes; deliberately does *not* auto-resume when the tab becomes visible again, so the player controls exactly when they jump back in rather than being dropped back into a moving snake with no warning. Added `aria-live="polite" aria-atomic="false"` to the toast layer so score/reset/speed-up feedback now reaches screen readers. Verified via Playwright: hiding the tab mid-game now sets `paused=true` automatically; becoming visible again does *not* auto-resume (stays paused until the player acts); a manual unpause afterwards still works and the game continues normally with no crash or corrupted state. No console errors beyond the sandbox's known inability to reach GitHub's raw content host and Supabase (both fine on the real deployment). |
| WordDrop | 🛠 | Another canvas/`requestAnimationFrame` game (falling words sorted into part-of-speech columns), and it turned up the most interesting *content-rendering* bug of the audit so far. The sentence markup wraps the target word in `___word___` and the renderer does `sentence.replace('___' + word + '___', SLOT_TOKEN)` to cut a blank slot out of it — this only works if the marked span is *exactly* the `word` field. A programmatic sweep of all 396 sentences (checking that the marked span equals the word field, case-insensitively) found 6 that didn't: e.g. `"She ___hard___ly spoke."` with `word:'hardly'` — the marker only wraps "hard", not "hardly", so the `replace()` silently fails to match and the sentence is shown to the player completely unprocessed, underscores and all ("She ___hard___ly spoke." verbatim), with no blank slot at all — meaning the word-in-progress "flies back" to a generic fallback position instead of its real spot in the sentence when answered correctly. One of the six (`"He ___late___d the application."`, `word:'lated'`) was worse than a markup slip: **"lated" isn't an English word at all** — there's no verb "to late" — so this entry was permanently unplayable/nonsensical regardless of markup. A second (`"He ___turned___ ___sharp___ly left."`, `word:'sharply'`) had a stray leftover `___turned___` marking from what looks like an unrelated earlier draft of the sentence, on top of the same "sharp"/"sharply" mismatch. Also found: (1) no `visibilitychange` handling — with only 3 lives total (compare WordSnake's 5 more-forgiving resets), a word left falling unseen in a backgrounded tab risks costing a real, scarce life for a miss the player never saw happen. (2) A narrow but real edge-case race in the manual "instant drop" control: pressing the drop key/button schedules scoring 50ms later via `setTimeout(() => landBlock(block.colIdx), 50)`, but `block.colIdx` is read *inside* the timeout — so an arrow-key press in that 50ms window (after the word visually looks like it's landed) could silently change which column the drop actually gets scored against. (3) No text feedback anywhere for a screen reader — every signal in this game (correct/wrong flash on the column label, the word flying back into the sentence, a life-loss icon animating) is purely visual/canvas-driven, so a non-visual player had no way to know what just happened on any given drop. Everything else checked out: no duplicate sentences across the 396-entry bank, mixed part-of-speech categories (73 nouns/85 verbs/75 adjectives/82 adverbs/42 prepositions/39 conjunctions) all read correctly in context on a full manual pass, and the deliberate "same stem, different part of speech" teaching device (close/closed, wrong/wronged, near/neared, sharp/sharply, etc.) is a genuinely good design once its broken entries are fixed. **Flagging, not changing:** `"She came ___even___ though it was late."` marks only "even" as the answer-word tagged `conjunction` — "even though" is usually treated as a single compound conjunction, with "even" alone arguably more of an intensifying adverb; a defensible design choice either way, not an objective error, so noting it for your call rather than unilaterally rewriting it. | Fixed the marker span on the 5 salvageable mismatches so each `___..___` wrapper exactly matches its `word` field (hardly, closed, wronged, neared, sharply — including stripping the stray extra `___turned___` marking from the "sharply" sentence). Replaced the impossible "lated" entry outright with a real, natural sentence at the same difficulty/topic — `"He ___delayed___ the application."` (word: delayed, verb). Added a `visibilitychange` listener that auto-pauses the moment the tab is hidden mid-game (reusing the existing `togglePause()`, so it doesn't auto-resume — the player controls when they jump back in). Fixed the drop-race by capturing `block.colIdx` into a local `landingCol` at the moment the player commits to dropping, and scoring against that captured value instead of re-reading the (still-mutable) `block.colIdx` when the timeout fires. Added a visually-hidden `aria-live="polite"` status region (reusing the site's existing `.sr-only` utility class) that now announces the outcome of every drop in plain language, e.g. *"Correct — 'closed' is a Verb. +15 points."* / *"Not quite — 'wronged' is a Verb, not a Noun. Life lost."* Verified via Playwright: all 6 previously-broken sentences now have markers that exactly match their word field and render correctly; the "lated" non-word no longer appears anywhere in the bank; hiding the tab mid-game auto-pauses (and does not auto-resume); a manual unpause afterwards works cleanly with the game continuing normally; the new aria-live region fires with the correct text on a scored drop. No console errors beyond the sandbox's known inability to reach Supabase. |
| Affix Runner | 🛠 | Completing the full gameplay audit (leaderboard-submit UX was already fixed early in the shared.js pass — see that row). This is a real-time `requestAnimationFrame` endless-runner (jump/duck to collect correctly-affixed words, dodge incorrectly-affixed ones), architecturally similar to WordSnake/WordDrop. Found: (1) no `visibilitychange` handling — words keep scrolling toward the runner unseen while the tab is backgrounded, risking an unseen collision with a wrong word (costing one of a limited pool of lives) or an unseen miss of a correct one (costing the streak) with the player unable to react. (2) No text feedback anywhere for a screen reader — entirely canvas/visual (score pop-ups, particle bursts, shake). Content: ran a full programmatic cross-check of all 193 prefix/suffix word entries (132 prefix + 61 suffix) against real system dictionaries (American + British, ~180k words) in both directions — every word marked "correct" that should be checked for being real, and every word marked "wrong" that should be checked for accidentally also being real — and found exactly one genuine miss: **USELESSNESS** was marked `correct:false` (grouped with the deliberately-invalid "wrong suffix combination" distractors like HOPEFULLESS/KINDNESSER), but "uselessness" (useless + -ness) is a completely standard, real English word — a student who correctly recognised it as valid and tried to collect it would have been told to dodge it instead, exactly the "legitimate answer marked wrong" pattern flagged as a priority for this audit. (One dictionary near-miss, "precooked," was investigated and is a real, standard word too — just missing from the particular system word list used for checking, a false positive in the check itself, not a bug in the game.) Everything else checked out: the ~30 invalid prefix/suffix combinations (DISUNHAPPY, MISUNLOCK, IRRESTART, etc.) and ~20 invalid suffix combinations (CAREFULL, HOPEFULLESS, KINDNESSER, etc.) are all genuinely non-words; a stray multi-word entry (`'CAREFUL NESS'`) used for internal documentation purposes is correctly filtered out of actual gameplay by the game's own `.filter(w => !w.word.includes(' '))` and never reaches a player. | Reclassified USELESSNESS as `correct:true` (moved it into the "-NESS valid" group, out of the wrong-suffix distractor block, with a comment explaining why). Added a `visibilitychange` listener that auto-pauses the run the moment the tab is hidden (reusing the existing `togglePause()`, which already correctly re-anchors its frame-timing reference on manual resume) — deliberately doesn't auto-resume, so the player controls when they jump back in. Added a visually-hidden `aria-live="polite"` status region for the meaningful, infrequent milestones only — life lost, round complete, game over — deliberately *not* narrating every individual word pickup, since at full speed that would be many events per second and pure noise for a screen-reader user; per-pickup feedback stays visual-only, same limitation as the other fast real-time games in this audit. Verified via Playwright: USELESSNESS now reads `correct:true`; hiding the tab mid-run auto-pauses and does not auto-resume; a manual unpause afterwards continues the run cleanly with no crash; the aria-live region correctly announces a sample life-lost message. No console errors beyond the sandbox's known inability to reach Supabase. |
| English Ace | 🛠 | Leaderboard submit UX was already fixed early in the shared.js pass (see that row). This is the largest real-time game in the set (`requestAnimationFrame`-driven "runner" over a 422-question hand-curated bank spanning collocations, phrasal verbs, past-simple/past-participle irregular verbs, spelling, synonyms and opposites), architecturally the same family as WordSnake/WordDrop/Affix Runner. Mechanics: (1) no `visibilitychange` handling — words keep spawning and scrolling toward the player unseen while the tab is backgrounded, risking an unseen life loss with no way to react. (2) No text feedback anywhere for a screen reader — score, wave, and life changes were entirely canvas/visual. Content, found via a full programmatic sweep of all 422 questions (not spot-checking): (1) a systemic rendering bug — 30 distractor strings across the bank carried a stray literal trailing digit (e.g. `'BLOWED2'`, `'DENTIST2'`, `'MINIMIZE2'`), clearly the leftover of a manual deduplication pass, and nothing in the rendering code (`ctx.fillText`) strips it — so players were seeing genuinely broken tokens like "BLOWED2" and "DENTIST2" fly across the screen as if they were real answer choices. (2) Nested inside that same dedup hack was a cluster of the exact "legitimate answer marked wrong" bug Kris flagged as a priority: `BURNED` marked wrong for "Past simple/participle of BURN" (both `BURNT` and `BURNED` are standard — BrE prefers `BURNT`, AmE `BURNED`); `DREAMED` marked wrong for "Past simple/participle of DREAM" (same BrE/AmE split with `DREAMT`); `LEARNED` marked wrong for "Past simple/participle of LEARN" (same split with `LEARNT`); `MINIMIZE` marked wrong for "Spell: to make smaller" (`MINIMISE`/`MINIMIZE` is a BrE/AmE spelling pair, not an error). Each of these had been given a deduplicating trailing digit rather than being removed outright, which is how they surfaced in the same sweep as the rendering bug. Everything else checked out: zero duplicate values within any single question's `{correct, wrong}` set across all 422 entries; a full dictionary cross-check of every spelling/verb-form "wrong" answer confirmed the large majority are intentional, pedagogically valid cross-tense-form distractors (e.g. `WRITTEN` correctly marked wrong for "Past simple of WRITE," since it's the participle, not the simple form) rather than bugs, so those were deliberately left unchanged; `awaked` was checked and judged archaic/non-standard in modern English (unlike the burn/dream/learn/minimise cases, which are fully standard everyday alternatives), so it was also left as-is. | Added a `visibilitychange` listener that auto-pauses the run the moment the tab is hidden (reusing the existing `togglePause()`, which already correctly re-anchors its frame-timing reference on manual resume) — deliberately doesn't auto-resume, so the player controls when they jump back in. Added a visually-hidden `aria-live="polite"` status region announcing the meaningful, infrequent milestones only — wave complete (with running score), a wrong hit (with lives remaining), and game over (with final score and wave reached) — deliberately not narrating every individual token, same reasoning as the other fast real-time games in this audit. Fixed all 30 digit-suffix tokens: 25 were safe to simply strip the stray digit from (they didn't collide with any sibling value); 5 needed a genuinely new replacement word instead, either because stripping would have collided with an existing distractor/the correct answer itself (`BLOWED2`→`BLEWN`, `DENTIST2`→`DENTIS`, `TOURIST2`→`TOURIS`) or because the digit was itself masking a dialectal-ambiguity bug (`MINIMIZE2`, `DREMT2` — see next). Fixed the burn/dream/learn/minimise cluster by replacing each wrongly-marked legitimate alternative with a genuinely invalid distractor instead of just stripping its digit, so both bugs (the visible "2" and the wrongly-marked real word) are resolved together. Verified via Playwright (9 assertions): the `aria-live` region is present and `polite`; zero digit-suffixed tokens remain anywhere in the bank (confirmed the only remaining `[A-Z]\d` matches are unrelated musical note names used by the game's Tone.js music system, correctly excluded); none of the six burn/dream/learn questions still list `BURNED`/`DREAMED`/`LEARNED` as wrong; zero duplicate values remain within any single question; the game starts into `state==='playing'`; hiding the tab auto-pauses; the game stays paused on return (no auto-resume); a manual unpause resumes play; and the game runs stably for a further second with no crash. No console errors beyond the sandbox's known inability to reach Supabase. **Not fully re-verified:** the remaining ~390 non-flagged "wrong" answers were reviewed as a batch against dictionary matches rather than each hand-checked against a grammar reference individually — flagging as ongoing, same caveat as the other content-heavy games. |
